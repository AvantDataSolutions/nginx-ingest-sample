For new setup:

    1) chmod +x init.sh
    2) [sudo] ./init.sh
    3) cp .env.tpl .env
    4) nano .env
        (set)
        POSTGRES_USER=
        POSTGRES_PASSWORD=
        PGADMIN_PASSWORD=
    5) docker-compose build
    6) docker-compose up -d

    7) Services should be up at port defined in .env
